class Solution {
    // this function always returns true because every array can be converted into a uniform parity array.
    public boolean uniformArray(int[] nums1) {
        return true;
        
    }
}